// DisplayPlaceLib removed — no longer used in SkyLight-only build.
// If you need the old displayplacer parsing utilities, restore them from
// the previous commits or reintroduce the DisplayPlaceLib target.
